import styled from "@emotion/styled";

export default styled.article`
  display: grid;
  grid-template-columns: 1fr 1fr;
  height: 100vh;

  font-family: "Cloak";
  font-weight: normal;
  font-style: normal;
`;
